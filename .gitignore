# Secrets / local env
.env
.env.*
# but DO track the documented template (no secrets in it):
!.env.example

# Python
.venv/
.venv-iq/
.iq-lite.log
.iq-lite.pid
.dash.log
.dash.pid
__pycache__/
*.pyc

# Local sims / scratch
sims/

# Machine-specific local Claude Code settings. Keep local; don't publish.
.claude/settings.json
.claude/settings.local.json

# OS
.DS_Store
tally.json
tally.json.tmp
dashboard/tally.json
