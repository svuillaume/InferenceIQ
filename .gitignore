# Secrets / local env
.env
.env.*
# but DO track the documented template (no secrets in it):
!.env.example

# Python
.venv/
__pycache__/
*.pyc

# Local sims / scratch
sims/

# Machine-specific local Claude Code settings (holds an absolute hook path; the plugin
# replaces this for distribution). Keep local; don't publish.
.claude/settings.json
.claude/settings.local.json

# OS
.DS_Store
tally.json
tally.json.tmp
dashboard/tally.json
